<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
  </head>
  <body style="max-width: 720px; margin: 40px auto; font-family: -apple-system, BlinkMacSystemFont, Helvetica, sans-serif; line-height: 1.6; padding: 0 20px;">
    <div style="text-align: center;">
      <img src="/img/avatar.jpg" alt="头像" style="width: 80px; border-radius: 50%; margin-bottom: 10px;" />
      <h1>{{ title }}</h1>
    </div>

    {{ content | safe }}

    <hr />
    <ul>
      {% for post in collections.posts | reverse %}
        <li>
          {{ post.date | date: "%y.%m.%d" }} -
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </li>
      {% endfor %}
    </ul>
  </body>
</html>
